{
  "name": "line-receipt-bot",
  "version": "2.0.0",
  "type": "module",
  "description": "LINE Bot for automatic receipt scanning — per-user Google OAuth",
  "main": "index.js",
  "scripts": {
    "start": "node --env-file=.env index.js",
    "dev": "node --env-file=.env --watch index.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.0",
    "@fastify/static": "^8.0.0",
    "@line/bot-sdk": "^9.5.0",
    "better-sqlite3": "^11.0.0",
    "fastify": "^5.8.4",
    "googleapis": "^140.0.1",
    "sqlite3": "^6.0.1"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
